% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize_ae.R
\name{summarize_ae_data}
\alias{summarize_ae_data}
\title{Summarize Adverse Event Data}
\usage{
summarize_ae_data(adsl, adae)
}
\arguments{
\item{adsl}{A data.frame of subject-level data, like the one generated
by \code{random.cdisc.data::radsl()}. Must contain columns \code{USUBJID} and \code{ARM}.}

\item{adae}{A data.frame of adverse event data, like the one generated
by \code{random.cdisc.data::radae()}. Must contain \code{USUBJID} and \code{AETERM}.}
}
\value{
A summarized data.frame with one row per adverse event term (\code{AETERM}).
Columns are created for the count (\code{n}) and percentage (\code{pct}) of subjects
in each treatment arm.
}
\description{
Calculates the number and percentage of subjects experiencing each adverse
event, summarized by treatment arm. The final output is a wide-format
table suitable for reporting.
}
\examples{
# This is an example of how the function can be used
adsl_data <- radsl(N = 100, seed = 1)
adae_data <- radae(adsl = adsl_data, seed = 1)
summarize_ae_data(adsl = adsl_data, adae = adae_data)
}
